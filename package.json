{
  "name": "tonyghouse-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -R site/. dist/ && tailwindcss -i ./src/styles.css -o ./dist/styles.css --minify",
    "dev": "npm run build && wrangler pages dev dist"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3",
    "wrangler": "^4.124.0"
  }
}
